From e906d6714af3a6557430b4810dfde8c95cc88aa1 Mon Sep 17 00:00:00 2001
From: Jonas Gorski <jonas.gorski@bisdn.de>
Date: Wed, 15 Jun 2022 17:45:38 +0200
Subject: [PATCH] CMakelists.txt: allow building the grpc_cli utility only

Upstream-Status: Pending

Signed-off-by: Jonas Gorski <jonas.gorski@bisdn.de>
---
 CMakeLists.txt | 19 +++++++++++++++++--
 1 file changed, 17 insertions(+), 2 deletions(-)

diff --git a/CMakeLists.txt b/CMakeLists.txt
index b84753bcb8d3..69558944106d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -5439,7 +5439,7 @@ foreach(_hdr
 endforeach()
 
 endif()
-if(gRPC_BUILD_TESTS)
+if(gRPC_BUILD_TESTS OR gRPC_BUILD_CLI)
 
 add_library(grpc++_test_config
   test/cpp/util/test_config_cc.cc
@@ -5489,6 +5489,14 @@ target_link_libraries(grpc++_test_config
   gpr
 )
 
+if(gRPC_INSTALL)
+  install(TARGETS grpc++_test_config EXPORT gRPCTargets
+    RUNTIME DESTINATION ${gRPC_INSTALL_BINDIR}
+    BUNDLE DESTINATION  ${gRPC_INSTALL_BINDIR}
+    LIBRARY DESTINATION ${gRPC_INSTALL_LIBDIR}
+    ARCHIVE DESTINATION ${gRPC_INSTALL_LIBDIR}
+  )
+endif()
 
 endif()
 if(gRPC_BUILD_TESTS)
@@ -19123,7 +19131,7 @@ target_link_libraries(grpc_byte_buffer_reader_test
 
 
 endif()
-if(gRPC_BUILD_TESTS)
+if(gRPC_BUILD_TESTS OR gRPC_BUILD_CLI)
 
 add_executable(grpc_cli
   ${_gRPC_PROTO_GENS_DIR}/src/proto/grpc/reflection/v1alpha/reflection.pb.cc
@@ -19174,6 +19182,13 @@ target_link_libraries(grpc_cli
   grpc++_test_config
 )
 
+if(gRPC_INSTALL)
+  install(TARGETS grpc_cli EXPORT gRPCTargets
+    RUNTIME DESTINATION ${gRPC_INSTALL_BINDIR}
+    LIBRARY DESTINATION ${gRPC_INSTALL_LIBDIR}
+    ARCHIVE DESTINATION ${gRPC_INSTALL_LIBDIR}
+  )
+endif()
 
 endif()
 if(gRPC_BUILD_TESTS)
-- 
2.53.0

